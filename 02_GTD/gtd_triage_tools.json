[
  {
    "type": "function",
    "function": {
      "name": "append_to_inbox",
      "description": "Append a captured task to the GTD inbox as a Markdown checkbox.",
      "parameters": {
        "type": "object",
        "properties": {
          "task_content": {
            "type": "string",
            "description": "The task or open loop to capture."
          },
          "context_tags": {
            "type": "array",
            "description": "Optional GTD context tags without # symbols.",
            "items": {
              "type": "string"
            },
            "default": []
          },
          "inbox_path": {
            "type": "string",
            "description": "Inbox path relative to the vault root.",
            "default": "00_Inbox.md"
          }
        },
        "required": ["task_content"],
        "additionalProperties": false
      }
    }
  }
]
